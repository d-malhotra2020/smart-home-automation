SECRET_KEY=your-secret-key-here-change-in-production
PORT=5000
DEBUG=False

# MQTT broker (optional). When unset, app runs in sim mode and the
# UI states this honestly. Set MQTT_HOST to wire a real broker.
MQTT_HOST=localhost
MQTT_PORT=1883
# MQTT_USERNAME=
# MQTT_PASSWORD=
# MQTT_CLIENT_ID=smart-home-app
